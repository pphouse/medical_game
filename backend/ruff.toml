line-length = 100
target-version = "py312"
extend-exclude = ["*/migrations/*"]

[lint]
# E731 included on purpose (spec §5-8: no lambda assignments).
select = ["E", "F", "W", "I", "B", "DJ", "UP"]
ignore = ["E501"]
