# Dev/CI: everything the runtime + scripts need, plus test/lint tooling.
-r requirements-scripts.txt
pytest==9.1.1
pytest-django==4.12.0
# pytest.ini から環境変数を渡すため（DJANGO_SECRET_KEY）。
pytest-env==1.7.0
ruff==0.15.22
